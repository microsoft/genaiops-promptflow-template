env_name: pr
experiment_config:
  name: math_standard_flow

  datasets:
  - name: math_coding_pr
    source: data/data.jsonl
    description: "This dataset is for pr validation only."
    mappings:
      url: "${data.url}"

  evaluators: