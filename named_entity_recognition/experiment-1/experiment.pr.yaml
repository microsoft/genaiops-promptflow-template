name: named_entity_recognition

datasets:
- name: temp_dataset
  source: data/data.jsonl
  description: "This dataset is for pr validation only."

evaluators:

# In this case the overlay is simple, we just swap the dataset source, all other parameters are read from the base (experiment.yaml)